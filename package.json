{
  "name": "mysignature",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "delete:reports": "rimraf cypress/results",
    "mochawesome:merge": "npx mochawesome-merge cypress/results/mochawesome/*.json > mochawesome.json && npx marge mochawesome.json",
    "prereport": "npm run delete:reports",
    "reportt": "cypress run --reporter cypress-multi-reporters --reporter-options configFile=reporter-config.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run_spec": "npx cypress run --spec 'cypress/e2e/InvalidLogin.spec.js' --browser chrome --headed"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.5.0",
    "cypress-multi-reporters": "^2.0.5",
    "dotenv": "^17.2.2",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "playwright-webkit": "^1.55.0",
    "rimraf": "^6.0.1"
  }
}
